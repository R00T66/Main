local Base64 = { }
local Synapse64 = syn.crypt.base64

function Base64:Encode(String)
   return Synapse64.encode(String)
end

function Base64:Decode(String)
   return Synapse64.decode(String)
end

return Base64
